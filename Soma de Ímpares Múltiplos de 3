cont = 0  # Contador de valores múltiplos de 3
soma = 0  # Acumulador da soma

for c in range(1, 501, 2):  # Loop que percorre apenas números ímpares de 1 a 500
    if c % 3 == 0:  # Verifica se o número é múltiplo de 3
        soma += c  # Acumula o valor na soma total
        cont += 1  # Conta quantos valores foram somados

print('A soma de {} valores é igual a {}'.format(cont, soma))  # Exibe o resultado final
